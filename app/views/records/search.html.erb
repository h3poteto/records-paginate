<h1>Listing Records</h1>

全部で：<%= @guess.count %>件
<table>
  <thead>
    <tr>
      <th>Id</th>
      <th>Category</th>
      <th>Title</th>
      <th>Description</th>
    </tr>
    <tr>
  </thead>
  <tbody>
    <% @guess.records.each do |r| %>
      <tr>
        <td><%= r.id %></td>
        <td><%= r.category_id %></td>
        <td><%= r.title %></td>
        <td><%= r.description %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= paging(@guess) %>
